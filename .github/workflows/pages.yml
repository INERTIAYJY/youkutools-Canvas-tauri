name: Deploy site

on:
  push:
    branches:
      - master
    paths:
      - 'site/**'
      - 'src/**'
      - 'index.html'
      - 'package-lock.json'
      - 'vite.config.ts'
      - '.github/workflows/pages.yml'
  workflow_dispatch:

# 只保留一次进行中的部署，不打断已经开始的那次
concurrency:
  group: pages
  cancel-in-progress: false

permissions:
  contents: read
  pages: write
  id-token: write

jobs:
  deploy:
    name: Deploy to GitHub Pages
    runs-on: ubuntu-latest
    environment:
      name: github-pages
      url: ${{ steps.deployment.outputs.page_url }}

    steps:
      - uses: actions/checkout@v4

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: 22
          cache: npm

      - name: Install npm dependencies
        run: npm ci

      # base 用相对路径，站点挂在仓库子路径还是自定义域名都不用改
      - name: Build web app for the in-page demo
        run: npx vite build --base=./ --outDir site/app --emptyOutDir

      # 首次部署前需要在 Settings > Pages 把 Source 选成 GitHub Actions。
      # 这一步不能自动化：configure-pages 的 enablement 参数明确要求提供
      # GITHUB_TOKEN 以外的令牌（PAT 需 repo scope），不值得为此长期存一个 PAT。
      - name: Configure Pages
        uses: actions/configure-pages@v5

      - name: Upload site artifact
        uses: actions/upload-pages-artifact@v3
        with:
          path: site

      - name: Deploy
        id: deployment
        uses: actions/deploy-pages@v4
