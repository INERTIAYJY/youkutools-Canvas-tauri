## ✨ 版本摘要

AI Canvas v0.6.11 新增可配置的只读领域子智能体，并将多图编辑器扩展为包含图层、画笔、滤镜、对齐和历史操作的轻量修图工作区。

## 🧠 对话助手与子智能体

- 新增用户可配置的只读领域子智能体，支持绑定 Skill 或自定义提示词、选择可读材料，并由主任务并行分工；内置剧本分析师和分镜师可复制为自定义配置。
- 子智能体配置入口移至 AI 助手顶部工具栏，并统一列表、表单、按钮和状态色的视觉样式。
- 厂商连接设置新增“调用助手添加”，可将接口文档预填到对话输入框并生成待确认的模型配置草稿。
- 助手向已有厂商连接添加模型时会合并而非覆盖原配置，并明确展示新增、更新和保留数量；Base URL 不一致时会拒绝合并。
- MCP 不再开放会产生额外模型调用成本的子智能体工具，避免外部客户端绕过任务预算触发调用。
- 收紧对话输入框内边距，为消息编辑区域留出更多空间。

## 🖼️ 多图编辑

- 属性面板支持整体收起，画布、工作区留白和底部命令栏会随面板宽度自动调整，不再遮挡编辑内容。
- 新增画笔、透明橡皮、亮度/对比度/饱和度等滤镜，以及图层混合模式、锁定、改名、缩略图和拖拽排序。
- 新增数值变换、翻转、六向对齐、适配/铺满、边缘与中线吸附、80 步撤销重做，并补齐常用编辑快捷键。

## 🤖 模型与连接

- 扩充 GRSAI 图片与多模态文本模型目录，新增 GPT Image、Nano Banana、GPT 和 Gemini 系列选项，并优先使用国内节点。
- 完善接口错误信息解析和 API Key 使用提示，GRSAI 设置可直接打开 API Key 页面。

## 🧩 文件与基础体验

- 修复 Windows 特殊路径前缀导致项目素材改名、分组移动和相对路径索引失效的问题；分组内文件改名后会保留原目录。
- 从画布节点录入角色时默认新建角色，并使用节点名称预填角色名；切换角色库范围时保留合理的录入模式。
- Toast 改为顶层 Portal 渲染，避免被面板或弹窗层级遮挡。

## ⚠️ 升级与兼容性

- 项目数据会自动升级到 IndexedDB v17，仅新增子智能体配置存储，不会重置现有项目数据。
- 使用 GRSAI 旧默认地址或全球默认地址的内置连接会自动迁移到当前国内节点；其他自定义 Base URL 保持不变。
- Intel Mac 版本不包含本地 ONNX Runtime，本地 ONNX 图像处理能力不可用；其他画布与 AI 模型功能不受影响。
- 浏览器模式无法使用桌面端凭据存储，API Key 仅在当前会话有效，重启后需要重新填写。

## 📦 下载

- 🪟 Windows：使用 `.exe` 或 `.msi`。
- 🍎 macOS：Apple Silicon 使用 `aarch64.dmg`，Intel 使用 `x64.dmg`。
- 🐧 Linux：使用 `.AppImage`、`.deb` 或 `.rpm`。
