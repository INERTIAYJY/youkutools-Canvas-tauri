🔧 设置交互与布局修复

## ✨ 版本摘要

AI Canvas v0.6.5 聚焦设置交互体验优化、样式计算统一与稳定性修复。

## 🔧 设置与交互优化

- 重构输出默认项配置，改用画布点击关闭对话框，交互更自然。
- 修复菜单定位偏差、IndexedDB 竞态条件与设置保存串行化问题。

## 🧪 样式与布局

- 提取宽高比尺寸计算逻辑并统一应用到各节点，避免分散实现导致的不一致。

## 📦 下载

请在下方 Assets 中选择对应平台的安装包：Windows 使用 `.exe` 或 `.msi`，macOS Apple Silicon 使用 `.dmg`，Linux 使用 `.AppImage`、`.deb` 或 `.rpm`。
