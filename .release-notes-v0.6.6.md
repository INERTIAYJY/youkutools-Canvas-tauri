🎭 角色库与创作偏好

## ✨ 版本摘要

AI Canvas v0.6.6 新增全局角色库和媒体提示词偏好学习，并完善资产与画布交互体验。

## 🎭 角色库

- 新增全局角色库，支持角色资料、多张参考图和项目间复用。
- 支持头像裁切、从画布节点捕获角色素材，以及全局资产重命名。
- 完善角色数据持久化和旧项目数据兼容读取。

## 🧠 创作偏好

- 对话助手可从用户确认的媒体提示词中学习创作偏好，用于后续上下文组装。

## 🧩 资产与画布

- 优化资产标签栏溢出展开交互。
- 优化节点工具栏缩放补偿与默认分区顺序。
- 修复画布右键菜单定位，并改善 macOS 窗口最大化行为。

## 📦 下载

请在下方 Assets 中选择对应平台的安装包：Windows 使用 `.exe` 或 `.msi`，macOS Apple Silicon 使用 `aarch64.dmg`，Intel 使用 `x64.dmg`，Linux 使用 `.AppImage`、`.deb` 或 `.rpm`。
