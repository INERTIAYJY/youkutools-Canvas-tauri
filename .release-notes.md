# AI Canvas v0.8.5

## ✨ 版本摘要

AI Canvas v0.8.5 统一项目模型路由与视觉上下文，让每个项目可固定选择自己的助手模型，并在对话时自动带入画布视觉信息；同时支持手动修改模型分类并持久化，优化 Windows 平台字体渲染，修复 asset URL 二次编码导致的保存失败等问题。

## 🤖 项目模型路由与视觉上下文

- 每个项目可固定选择自己的助手模型，统一项目模型路由，对话与 Agent 默认使用项目指定模型。
- 新增画布视觉上下文，对话时自动提取并描述当前画布视觉信息，帮助模型理解画面内容。
- 新增视觉描述服务，将画布节点与图片内容转为可理解的文字描述带入对话。

## ⚙️ 模型分类管理

- 模型分类标签改为按钮，点击可展开分类选择器，支持手动修改模型分类。
- 手动指定分类后重新拉取或合并目录不再覆盖，minimax-h3 系列视频模型分类推断规则优化。

## 🖥️ 界面优化

- 优化 Windows 平台字体渲染与设置项最小字号。
- 修复画布缩放时按钮视觉尺寸偏移问题。
- 修复独立窗口模式下吉祥物关闭逻辑。

## 🌐 官网

- 官网下载页从 GitHub Releases 动态获取最新版本号，无需手动更新。

## 🐛 修复

- 修复 asset URL 二次编码导致保存失败的问题，统一处理 asset、blob、http(s) URL 并检查响应状态。
- 源文件读取失败时回退到节点 URL，避免整体失败。

## ⚠️ 升级与兼容性

- 项目级模型路由需要 v0.8.5 或更高版本，旧版本不包含项目视觉上下文能力。
- 视频剪辑器依赖本地编解码能力，实际可用导出编码格式取决于本机编解码器。
- 浏览器模式无法使用桌面端凭据存储，API Key 仅在当前会话有效，重启后需要重新填写。
- 视频剪辑窗口、应用内 ComfyUI、MCP 控制桥、长按圆环和本地文件导出仅在桌面版可用。

## 📦 下载

- 🪟 Windows：使用 `.exe` 或 `.msi`。
- 🍎 macOS：Apple Silicon 使用 `aarch64.dmg`，Intel 使用 `x64.dmg`。
- 🐧 Linux：使用 `.AppImage`、`.deb` 或 `.rpm`。
