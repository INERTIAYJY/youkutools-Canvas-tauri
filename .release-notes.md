# AI Canvas v0.8.8

## ✨ 版本摘要

AI Canvas v0.8.8 用变量总表统一自定义协议的参数映射，修正视频参考素材角色与中转参数对接，完善网页读取渲染与厂商文档读取能力，并清理死代码与重复实现、优化相同 Base URL 草稿并入逻辑。

## 🔧 协议参数映射统一

- 新增 modelProtocolVariables 作为唯一来源，协议变量白名单、中转文档字段识别、参考素材覆盖检查与可用变量列表全部由它派生，避免四份清单各自漂移。
- 字段匹配优先级改为按具体度打分，不再依赖 if 顺序。
- 新增 supplied 维度，运行时拿不到值的变量不参与字段匹配，避免映射后字段被整条省略。
- 连线带了参考素材但协议无对应字段时，生成前提示并标注「无参考素材字段」。
- 扩充中转站常见字段别名（batch_size / init_image / start_frame / speed_ratio 等）。

## 🎥 视频参考素材角色修正

- Seedance 2.0/2.5 挑了首/尾帧再连普通参考图时，参考图按 role=reference_image 并入 image_with_roles，不再塞进 image_urls 触发互斥报错。
- 参考图数量上限对 image_urls 与 image_with_roles 合计生效。

## 🌐 网页与文档读取增强

- 改进网页正文提取：SPA 检测、主内容区选择、渲染静默判定。
- 完善厂商文档读取能力，新增 providerDocsService 测试。

## ⚙️ 其他改进

- 相同 Base URL 草稿并入已有连接，跳过未变模型。
- 工作流管理新增默认节点提示。
- 清理死代码与重复实现，移除未使用的 motion 依赖。

## ⚠️ 升级与兼容性

- 协议变量总表需要 v0.8.8 或更高版本。
- 视频剪辑器依赖本地编解码能力，实际可用导出编码格式取决于本机编解码器。
- 浏览器模式无法使用桌面端凭据存储，API Key 仅在当前会话有效，重启后需要重新填写。
- 视频剪辑窗口、应用内 ComfyUI、MCP 控制桥、长按圆环和本地文件导出仅在桌面版可用。

## 📦 下载

- 🪟 Windows：使用 `.exe` 或 `.msi`。
- 🍎 macOS：Apple Silicon 使用 `aarch64.dmg`，Intel 使用 `x64.dmg`。
- 🐧 Linux：使用 `.AppImage`、`.deb` 或 `.rpm`。
