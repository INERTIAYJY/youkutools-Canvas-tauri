## ✨ 版本摘要

AI Canvas v0.8.0 把项目扩展成「剧集 + 分集」：一部短剧可以拆成多集，每集独占一张画布，角色库和素材整部剧共用，助手能读完剧本后直接批量建集。同时 ComfyUI 工作流支持设置默认输入输出节点，画布切换与自动保存的耗时大幅下降。

## 🎬 剧集与分集

- 项目支持拆分为剧集与分集，每一集独占一张画布，顶部标签和项目库仍然只列剧集本身。
- 新增右侧剧集栏：上传原著文件、填写剧本、增删改分集、调整集号顺序，并为每一集写本集大纲。
- 对话助手新增 `series_read` 与 `series_split_episodes` 两个工具，读完原著或剧本后可直接批量创建分集并写入大纲。
- 双击剧集栏竖线打开项目资产浮层，按集查看整部剧已生成的图片与视频。
- 角色库、项目记忆和素材目录整部剧共用一份，改一集的角色其他集同步可见。
- 标题栏显示「剧集名-集名」，项目库中剧集卡片的封面取第一集的画布缩略图。

## 🧩 ComfyUI 工作流

- 支持为工作流设置默认输入输出节点，提示词和媒体引用在未用 `@` 指定节点时自动注入到默认节点，不再依赖占位符猜测。
- 修复保存工作流后默认节点设置失效的问题，保存时会清理指向已删除节点的默认设置。
- 修复保存后工作流面板意外关闭，保存后可继续编辑。
- 应用内 ComfyUI 窗口的悬浮工具栏改为与主界面一致的玻璃拟态样式，顶部菜单栏移到页面底部。

## 🎥 视频生成

- 添加时未声明参数的接口模型，现在统一提供 480p 至 4K 分辨率、16:9 至自适应宽高比和按秒计的时长，不再退化为像素分辨率面板；ComfyUI 与 RunningHub 本地工作流保持原有的像素分辨率加帧率面板。
- 有声视频默认开启（火山方舟原生 Seedance 1.0 不支持音频，仍默认关闭）。

## ⚡ 画布切换与保存

- 切换画布时显示 Logo 加载动画，切换够快时不会闪烁。
- 自动保存不再对每个媒体节点重复校验本地文件，切换画布也不再全量扫描项目素材目录，节点越多提速越明显。
- 画布缩略图只在从项目库切换时重拍，标签栏和剧集栏之间切换不再触发截图。

## 🛠️ 修复

- 修正厂商配置工具的引导词，助手写完配置草稿后会立即发起审批，不再停在等待用户确认。

## ⚠️ 升级与兼容性

- 已有项目不受影响，仍是普通项目。第一次为某个项目新增分集时，该项目会转为剧集：原项目名成为剧集名，原画布原地变成「第 1 集」，画布内容、对话和输出历史都保留在这一集上。
- 分集与剧集共用同一个素材目录。只删除单独一集不会清理它产生的素材，需要清理时通过资产库回收；删除整部剧才会删除共享目录。
- 剧集本身没有画布，打开剧集等于打开它的第一集。
- 视频剪辑器依赖本地编解码能力，实际可用导出编码格式取决于本机编解码器。
- 浏览器模式无法使用桌面端凭据存储，API Key 仅在当前会话有效，重启后需要重新填写。
- 视频剪辑窗口、应用内 ComfyUI、MCP 控制桥和本地文件导出仅在桌面版可用。

## 📦 下载

- 🪟 Windows：使用 `.exe` 或 `.msi`。
- 🍎 macOS：Apple Silicon 使用 `aarch64.dmg`，Intel 使用 `x64.dmg`。
- 🐧 Linux：使用 `.AppImage`、`.deb` 或 `.rpm`。
