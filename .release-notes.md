# AI Canvas v0.8.2

## ✨ 版本摘要

AI Canvas v0.8.2 内置 MiniMax H3 全套视频工作流，新增长按快捷圆环和 Sprite Sheet 切帧导出，画布 Agent 工具集大幅增强（分集资产操作、动画管理、系列工具），并修复了拖动/缩放节点的撤销逻辑。

## 🎥 MiniMax H3 内置工作流

- 新增 4 套 MiniMax H3 内置 ComfyUI 工作流：图生视频、参考图生视频、文生视频及对应的 Turbo 变体，共 6 套 UI 工作流。
- MiniMax H3 视频生成支持宽高比、时长等参数选择，与现有视频参数面板统一。
- 工作流列表支持搜索与筛选，导出下载提供明确反馈。

## 🔵 长按快捷圆环

- 画布空白处长按唤出径向快捷菜单，可快速添加 AI 生成、分镜表、画布笔记等节点。
- 圆环支持动画展开与收起，选项根据画布上下文动态显示。

## 🎞️ Sprite Sheet 切帧导出

- 动画节点新增 Sprite Sheet 切帧导出功能，将逐帧动画导出为横向或纵向排列的精灵图。
- 支持调整列数、行数和帧间隔，导出结果新建图片节点回填画布。
- 动画节点新增逐帧播放控制，可逐帧查看动画序列。

## 🤖 画布 Agent 工具增强

- Agent 画布工具集大幅增强：新增分集资产查询、动画节点管理、系列分集操作等工具。
- 分集资产工具支持按集查询、筛选和管理图片/视频/音频素材。
- 工作流工具支持内置工作流发现与参数注入。

## 🛠️ 其他改进

- ComfyUI 工作流支持设置默认输入输出节点，提示词和媒体引用在未用 `@` 指定时自动注入默认节点。
- 画面挑选浮层放大，生成提示词支持 `@` 引用画布节点。
- macOS 下快捷键提示适配 Command 键显示。
- 改进消息列表自动滚动逻辑。

## 🐛 修复

- 修复拖动/缩放节点时撤销步创建时机与还原逻辑错误，撤销后不再丢失节点位置。
- 修正重做快捷键（Ctrl+Shift+Z / Cmd+Shift+Z）不生效的问题。
- 修复 ComfyUI 窗口打开保护，避免重复打开多个窗口。

## ⚠️ 升级与兼容性

- MiniMax H3 内置工作流需要 v0.8.2 或更高版本，旧版本不包含这些工作流。
- 长按快捷圆环和 Sprite Sheet 导出仅在桌面版可用。
- 视频剪辑器依赖本地编解码能力，实际可用导出编码格式取决于本机编解码器。
- 浏览器模式无法使用桌面端凭据存储，API Key 仅在当前会话有效，重启后需要重新填写。
- 视频剪辑窗口、应用内 ComfyUI、MCP 控制桥、长按圆环和本地文件导出仅在桌面版可用。

## 📦 下载

- 🪟 Windows：使用 `.exe` 或 `.msi`。
- 🍎 macOS：Apple Silicon 使用 `aarch64.dmg`，Intel 使用 `x64.dmg`。
- 🐧 Linux：使用 `.AppImage`、`.deb` 或 `.rpm`。
