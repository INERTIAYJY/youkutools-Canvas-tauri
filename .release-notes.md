# AI Canvas v0.8.4

## ✨ 版本摘要

AI Canvas v0.8.4 推出 MCP 全面控制能力，让外部客户端通过同一套工具与权限矩阵深度控制画布、项目、剧集、工作流与界面；同时新增 ComfyUI 内部助手动态工作流、Agent 可折叠执行依据视图，并支持 MiniMax H3 视频生成模型。

## 🔌 MCP 全面控制能力

- 新增项目管理、剧集会话任务与历史、工作流与内容管理、高级画布操作、界面窗口视口与截图等工具，外部 MCP 客户端可深度控制应用。
- 现有工具启用无确认全权限执行，C 模式与 MCP effect 策略同步更新。
- 新增连接环境说明，明确 MCP 控制桥的依赖与运行要求。

## 🖥️ ComfyUI 内部助手动态工作流

- 助手可通过 ComfyUI API 动态发现模型和节点，自行编写工作流并执行。
- 执行前需经过严格校验换取短期令牌，每次执行须用户确认，成功的工作流可按需保存到工作流管理。
- 支持终止 ComfyUI 生成任务，自动获取工作流名称，兼容新版 job cancel API 与旧版队列删除/中断。

## 🤖 Agent 执行依据与工具详情

- 新增可折叠执行依据视图，展示 Agent 每步决策的理由与依据。
- 增强工具调用参数与变更详情展示，参数、结果和改动一目了然。
- 固定显式 Skill 任务上下文，避免 Skill 任务上下文漂移。

## 🎥 MiniMax H3 视频生成

- ApiMart 新增 MiniMax H3 视频生成模型支持，接入统一媒体生成入口。

## ⚠️ 升级与兼容性

- MCP 全面控制能力需要 v0.8.4 或更高版本，旧版本仅包含基础工具集。
- 视频剪辑器依赖本地编解码能力，实际可用导出编码格式取决于本机编解码器。
- 浏览器模式无法使用桌面端凭据存储，API Key 仅在当前会话有效，重启后需要重新填写。
- 视频剪辑窗口、应用内 ComfyUI、MCP 控制桥、长按圆环和本地文件导出仅在桌面版可用。

## 📦 下载

- 🪟 Windows：使用 `.exe` 或 `.msi`。
- 🍎 macOS：Apple Silicon 使用 `aarch64.dmg`，Intel 使用 `x64.dmg`。
- 🐧 Linux：使用 `.AppImage`、`.deb` 或 `.rpm`。
